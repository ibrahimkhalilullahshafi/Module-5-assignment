version: '3.8'

services:
  express-app:
    build: .
    container_name: express-app
    expose:
      - "5000"
    depends_on:
      - nginx

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
